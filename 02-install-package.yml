- name: Install linux package
  hosts: catalogue
  become: yes
  tasks:
  - name: intsall nginx package
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Ensure Nginx service is running and enabled at boot
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes